AWSTemplateFormatVersion: "2010-09-09"

Description: |
  Setup ECS cluster

Resources:
  ECSCluster: #Logical name
      Type: 'AWS::ECS::Cluster'
      Properties:
        ClusterName: MyCluster
        CapacityProviders: 
          - FARGATE
# Parameters:
# Mappings:
# Outputs:
# Metadata: